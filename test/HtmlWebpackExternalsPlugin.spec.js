import HtmlWebpackExternalsPlugin from '../lib/'
import assert from 'assert'

describe('HtmlWebpackExternalsPlugin', function() {
  it('does something', function() {
    assert.equal(true, false, 'Tests are working!')
  })
})
